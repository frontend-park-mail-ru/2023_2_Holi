<div class="input-container {{#each containerClasses}} {{this}} {{/each}}">
    <input class="form-input {{#each inputClasses}} {{this}} {{/each}}" placeholder="{{placeholder}}" type="{{type}}">
</input-continer>
